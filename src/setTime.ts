const amountOfPiers = 4; // how many piers do we need
const createNewShip = 7000; // this much time we need to create a new ship
const timeBeforePort = 3500; // time we need between ship was createt and arrived to Port Line
const loopThroughArrays = 1000; // setInterval time for the loop through lines
const timeToGiveAwayLoad = 5000; // that much time ship will be in the port

export {
  amountOfPiers,
  createNewShip,
  timeBeforePort,
  loopThroughArrays,
  timeToGiveAwayLoad,
};
